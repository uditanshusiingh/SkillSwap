import { useState } from 'react';
import { createGig } from '../api';
import { useNavigate } from 'react-router-dom';
const categories=['Graphic Design','Video Editing','Photography','Content Writing','Web Development','Social Media','Music & Audio','Animation','Digital Marketing','UI/UX Design'];
export default function PostGig(){const nav=useNavigate();const [form,setForm]=useState({creatorName:'',title:'',category:'Graphic Design',rate:'',description:''});const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);setMsg('');try{await createGig(form);nav('/marketplace');}catch(err){setMsg(err.response?.data?.message||'Could not publish gig.');}finally{setBusy(false)}};return <main className="container py-5 narrow"><span className="eyebrow">FOR CREATORS</span><h1 className="mt-2">Post a gig</h1><p className="text-secondary">Package your skill into a clear service clients can book.</p><form className="form-card shadow-sm mt-4" onSubmit={submit}><label>Creator name<input required className="form-control" value={form.creatorName} onChange={e=>setForm({...form,creatorName:e.target.value})}/></label><label>Service title<input required className="form-control" placeholder="e.g. YouTube Thumbnail Design" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><div className="row"><div className="col-md-6"><label>Category<select className="form-select" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{categories.map(c=><option key={c}>{c}</option>)}</select></label></div><div className="col-md-6"><label>Rate (₹)<input required min="0" type="number" className="form-control" value={form.rate} onChange={e=>setForm({...form,rate:e.target.value})}/></label></div></div><label>Description<textarea required rows="5" className="form-control" placeholder="Explain exactly what the client receives..." value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label>{msg&&<div className="alert alert-danger">{msg}</div>}<button disabled={busy} className="btn btn-dark btn-lg w-100">{busy?'Publishing…':'Publish Gig'}</button></form></main>}
