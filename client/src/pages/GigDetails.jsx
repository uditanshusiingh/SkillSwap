import { useEffect,useState } from 'react';
import { useNavigate,useParams } from 'react-router-dom';
import { createBooking,getGig } from '../api';
export default function GigDetails(){const {id}=useParams(),nav=useNavigate();const [gig,setGig]=useState(null);const [form,setForm]=useState({clientName:'',clientEmail:'',requirements:'',preferredDate:''});const [done,setDone]=useState(false);const [error,setError]=useState('');useEffect(()=>{getGig(id).then(r=>setGig(r.data)).catch(()=>setError('Gig not found.'));},[id]);if(error)return <main className="container py-5"><div className="alert alert-danger">{error}</div></main>;if(!gig)return <main className="container py-5">Loading…</main>;const submit=async e=>{e.preventDefault();try{await createBooking({gigId:id,...form});localStorage.setItem('skillswapClientEmail',form.clientEmail);setDone(true);}catch(err){setError(err.response?.data?.message||'Could not create booking.')}};return <main className="container py-5"><div className="row g-5"><div className="col-lg-7"><span className="badge category">{gig.category}</span><h1 className="display-5 fw-bold mt-3">{gig.title}</h1><p className="text-secondary">Created by <strong>{gig.creatorName}</strong></p><div className="gig-description mt-4">{gig.description}</div><div className="price-large mt-5">₹{gig.rate}<small> / project</small></div></div><div className="col-lg-5">{done?<div className="success-card"><div className="check">✓</div><h3>Booking submitted</h3><p className="text-secondary">Your request is pending creator approval.</p><button className="btn btn-dark" onClick={()=>nav('/my-bookings')}>View My Bookings</button></div>:<form className="form-card shadow-sm" onSubmit={submit}><h3>Book this gig</h3><p className="text-secondary">Send your project details to {gig.creatorName}.</p><label>Your name<input required className="form-control" value={form.clientName} onChange={e=>setForm({...form,clientName:e.target.value})}/></label><label>Email<input required type="email" className="form-control" value={form.clientEmail} onChange={e=>setForm({...form,clientEmail:e.target.value})}/></label><label>Preferred date<input required type="date" className="form-control" value={form.preferredDate} onChange={e=>setForm({...form,preferredDate:e.target.value})}/></label><label>Requirements<textarea rows="4" className="form-control" placeholder="Tell the creator what you need…" value={form.requirements} onChange={e=>setForm({...form,requirements:e.target.value})}/></label>{error&&<div className="alert alert-danger">{error}</div>}<button className="btn btn-dark btn-lg w-100">Confirm Booking</button></form>}</div></div></main>}
